# How to run backend

requirements.txt is available to install required packages

step 1. cd backend

step 2. python init_db.py

step 3. python data_fetcher.py

step 4. python -m uvicorn app:app --reload